import { test, expect, jest } from "@jest/globals";

/**
 * index.ts does not contain testable code so this is an empty test
 * just for the feel of having one test that never fails.
 */
test("index.ts", () => {});
